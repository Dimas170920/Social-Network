let initialState = {
    names: [
        {id: 1, name: 'Dima'},
        {id: 2, name: 'Andrew'},
        {id: 3, name: 'Sasha'}
    ]
};

const navbarReducer = (state = initialState, action) =>{

    return state;
}

export default navbarReducer;